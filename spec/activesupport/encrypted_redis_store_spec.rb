RSpec.describe Activesupport::EncryptedRedisStore do
  it "has a version number" do
    expect(Activesupport::EncryptedRedisStore::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
